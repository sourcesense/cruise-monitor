- build is *still* broken, when after 30 minutes no-one has fixed it
+ don't notify success if build is still clean
+ don't update latest.build.info if nothing changed
- minimal configuration: server/port, project name
- support for CruiseControl
+ support for CruiseControl.NET
+ support for Hudson
+ support for Jenkins
- Project name with spaces (eg: 'ActiveMQ Protocol Buffer')
- multiple projects monitoring
- rdoc documentation
+ run on Windows
	+ all tests
	+ monitoring
	+ scheduling
+ deploy script
- built-in scheduling
+ rake task for sync
+ rake task for deploy
+ rake task for init configuration
+ wiki/docs under revision control
	- move pictures to cruise-monitor.wiki repository